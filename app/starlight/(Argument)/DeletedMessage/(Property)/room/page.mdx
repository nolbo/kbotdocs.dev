import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "DeletedMessage.room - StarLight",
    openGraph: {
        ...openGraph,
        title: "DeletedMessage.room - StarLight"
    }
}

<Section>
    ## DeletedMessage.room

    메시지가 삭제된 방에 대한 [`ChatRoom`](/starlight/ChatRoom) 객체입니다. 해당 프로퍼티는 `null`일 수 있습니다.
</Section>
<Section>
    ## 구문

    ~~~js
    deletedMsg.room;
    ~~~

    <Section>
        ### 값

        `ChatRoom?` - 메시지가 삭제된 방에 대한 `ChatRoom` 객체 또는 `null`입니다.
    </Section>
</Section>