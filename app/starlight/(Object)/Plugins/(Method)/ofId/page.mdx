export const metadata = {
    title: "Plugins.ofId() - StarLight",
    openGraph: {
        title: "Plugins.ofId() - StarLight"
    }
}

<Section>
    ## Plugins.ofId()

    특정 ID로 식별되는 플러그인에 대한 [`Plugin`](/starlight/Plugin) 객체를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Plugins.ofId(id: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`id`</Dt>
            <Dd>
                플러그인의 고유 ID
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `Plugin` - `id`로 식별되는 플러그인에 대한 `Plugin` 객체를 반환합니다.
    </Section>
</Section>