export const metadata = {
    title: "Project.getLanguage() - StarLight",
    openGraph: {
        title: "Project.getLanguage() - StarLight"
    }
}

<Section>
    ## Project.getLanguage()

    프로젝트에서 사용하는 언어에 대한 [`Language`](/starlight/Language) 객체를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Project.getLanguage();
    ~~~

    <Section>
        ### 반환값

        `Language` - 프로젝트에서 사용하는 언어에 대한 `Language` 객체를 반환합니다.
    </Section>
</Section>