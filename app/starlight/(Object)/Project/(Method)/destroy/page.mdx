export const metadata = {
    title: "Project.destroy() - StarLight",
    openGraph: {
        title: "Project.destroy() - StarLight"
    }
}

<Section>
    ## Project.destroy()

    작동 중인 모든 `job`을 중지합니다. 프로젝트가 컴파일된 상태일 경우, `langScope`를 해제합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Project.destroy();
    ~~~
</Section>