export const metadata = {
    title: "Project.activeJobs() - StarLight",
    openGraph: {
        title: "Project.activeJobs() - StarLight"
    }
}

<Section>
    ## Project.activeJobs()

    프로젝트의 쓰레드 풀에서 작동 중인 `job`의 수를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Project.activeJobs();
    ~~~

    <Section>
        ### 반환값

        `Number` - 프로젝트의 쓰레드 풀에서 작동 중인 `job`의 수를 반환합니다.
    </Section>
</Section>