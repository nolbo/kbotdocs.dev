import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Projects - StarLight",
    openGraph: {
        ...openGraph,
        title: "Projects - StarLight"
    }
}

<Section>
    ## Projects

    `Projects` 객체는 프로젝트에 접근하기 위한 객체입니다.
</Section>
<Section>
    ## 메소드

    <Table>
        <Thead>
            <Tr>
                <Th colSpan="2">`Projects` 객체의 메소드</Th>
            </Tr>
        </Thead>
        <Tbody>
            <Tr>
                <Td><span>[`Project`](/starlight/Project)</span></Td>
                <Td>
                    [`getSelf`](/starlight/Projects/getSelf)`()` \
                    프로젝트에 대한 `Project` 객체를 반환합니다.
                </Td>
            </Tr>
            <Tr>
                <Td><span>[`Project`](/starlight/Project)</span></Td>
                <Td>
                    [`ofId`](/starlight/Projects/ofId)`(id: String)` \
                    특정 ID로 식별되는 프로젝트에 대한 `Project` 객체를 반환합니다.
                </Td>
            </Tr>
            <Tr>
                <Td><span>[`Project`](/starlight/Project)</span></Td>
                <Td>
                    [`ofName`](/starlight/Projects/ofName)`(name: String)` \
                    특정 이름의 프로젝트에 대한 `Project` 객체를 반환합니다.
                </Td>
            </Tr>
        </Tbody>
    </Table>
</Section>