export const metadata = {
    title: "Projects.ofId() - StarLight",
    openGraph: {
        title: "Projects.ofId() - StarLight"
    }
}

<Section>
    ## Projects.ofId()

    특정 ID로 식별되는 프로젝트에 대한 [`Project`](/starlight/Project) 객체를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Projects.ofId(id: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`id`</Dt>
            <Dd>
                프로젝트의 고유 ID
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `Project` - `id`로 식별되는 프로젝트에 대한 `Project` 객체를 반환합니다.
    </Section>
</Section>