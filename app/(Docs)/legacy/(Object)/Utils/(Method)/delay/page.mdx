import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.delay() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.delay() - 레거시 API"
    }
}

<Section>
    # Utils.delay()

    일정 시간(밀리초; ms)만큼 지연합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.delay(mil: Number);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`mil`</Dt>
            <Dd>
                지연할 시간(밀리초; ms)
            </Dd>
        </Dl>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["delay"]}/>
</Section>