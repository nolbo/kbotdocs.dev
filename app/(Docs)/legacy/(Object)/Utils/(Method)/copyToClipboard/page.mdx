import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.copyToClipboard() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.copyToClipboard() - 레거시 API"
    }
}

<Section>
    ## Utils.copyToClipboard()

    문자열 `value`를 클립보드에 복사합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.copyToClipboard(value: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`value`</Dt>
            <Dd>
                클립보드에 복사할 문자열 값
            </Dd>
        </Dl>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["copyToClipboard"]}/>
</Section>