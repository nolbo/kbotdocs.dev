import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.measureNanoTime() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.measureNanoTime() - 레거시 API"
    }
}

<Section>
    # Utils.measureNanoTime()

    `func` 함수의 실행에 소요된 시간을 반환합니다.(나노초; ns)
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.measureNanoTime(func: () => Any?);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`func`</Dt>
            <Dd>
                실행 시간을 측정할 함수
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `Number` - `func` 함수의 실행에 소요된 시간을 반환합니다.(나노초; ns)
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["measureNanoTime"]}/>
</Section>