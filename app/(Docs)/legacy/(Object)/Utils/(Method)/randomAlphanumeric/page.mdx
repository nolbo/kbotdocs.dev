import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.randomAlphanumeric() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.randomAlphanumeric() - 레거시 API"
    }
}

<Section>
    # Utils.randomAlphanumeric()

    길이가 `length`인 영숫자(alphanumeric characters)를 무작위 생성해 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.randomAlphanumeric(length: Number);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`length`</Dt>
            <Dd>
                생성할 영숫자의 길이
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - 길이가 `length`인 영숫자를 무작위 생성해 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["randomAlphanumeric"]}/>
</Section>