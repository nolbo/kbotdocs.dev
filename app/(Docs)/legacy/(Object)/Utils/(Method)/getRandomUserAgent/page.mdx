import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.getRandomUserAgent() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.getRandomUserAgent() - 레거시 API"
    }
}

<Section>
    # Utils.getRandomUserAgent()

    `User-Agent` 요청 헤더 값을 무작위 생성해 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.getRandomUserAgent();
    ~~~

    <Section>
        ### 반환값

        `String` - `User-Agent` 요청 헤더 값을 무작위 생성해 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getRandomUserAgent"]}/>
</Section>