import {compatibility} from "@/constants/compatibility/legacy/object/Utils";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Utils.vibrate() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Utils.vibrate() - 레거시 API"
    }
}

<Section>
    ## Utils.vibrate()

    주어진 시간(초; s)동안 진동을 울립니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.vibrate(sec: Number);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`sec`</Dt>
            <Dd>
                주어진 시간(초; s)동안 진동을 울립니다.
            </Dd>
        </Dl>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["vibrate"]}/>
</Section>