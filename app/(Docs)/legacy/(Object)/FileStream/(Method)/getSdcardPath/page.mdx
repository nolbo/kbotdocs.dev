import {compatibility} from "@/constants/compatibility/legacy/object/FileStream";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "FileStream.getSdcardPath() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "FileStream.getSdcardPath() - 레거시 API"
    }
}

<Section>
    ## FileStream.getSdcardPath()

    내장 메모리의 최상위 경로를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    FileStream.getSdcardPath();
    ~~~

    <Section>
        ### 반환값

        `String` - 내장 메모리의 최상위 경로를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getSdcardPath"]}/>
</Section>