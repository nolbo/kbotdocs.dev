import {compatibility} from "@/constants/compatibility/legacy/object/Api";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Api.UIThread() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Api.UIThread() - 레거시 API"
    }
}

<Section>
    ## Api.UIThread()

    UI 쓰레드 상에서 특정 함수를 실행합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Api.UIThread(func: () => Any?, onComplete?: (error: java.lang.Throwable?, result: Any?) => Any?);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`func`</Dt>
            <Dd>
                UI 쓰레드 상에서 실행할 함수
            </Dd>
            <Dt>`onComplete?`</Dt>
            <Dd>
                `func` 함수의 결과를 처리할 함수
                - `error`: `func` 함수의 실행 도중 일어난 에러에 대한 `java.lang.Throwable`
                - `result`: `func` 함수의 반환값
            </Dd>
        </Dl>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["UIThread"]}/>
</Section>