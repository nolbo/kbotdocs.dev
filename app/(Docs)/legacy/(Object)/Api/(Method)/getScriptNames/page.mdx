import {compatibility} from "@/constants/compatibility/legacy/object/Api";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Api.getScriptNames() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Api.getScriptNames() - 레거시 API"
    }
}

<Section>
    # Api.getScriptNames()

    모든 스크립트의 이름을 배열로 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Api.getScriptNames();
    ~~~

    <Section>
        ### 반환값

        `String[]` - 모든 스크립트의 이름이 담긴 배열을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getScriptNames"]}/>
</Section>