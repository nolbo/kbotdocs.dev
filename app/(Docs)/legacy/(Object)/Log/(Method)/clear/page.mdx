import {compatibility} from "@/constants/compatibility/legacy/object/Log";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Log.clear() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Log.clear() - 레거시 API"
    }
}

<Section>
    # Log.clear()

    스크립트 로그를 모두 삭제합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Log.clear();
    ~~~
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["clear"]}/>
</Section>