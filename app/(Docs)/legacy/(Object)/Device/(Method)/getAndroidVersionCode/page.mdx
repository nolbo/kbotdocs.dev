import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getAndroidVersionCode() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.getAndroidVersionCode() - 레거시 API"
    }
}

<Section>
    ## Device.getAndroidVersionCode()

    앱 구동 환경의 안드로이드 버전 코드를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getAndroidVersionCode();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱 구동 환경의 안드로이드 버전 코드를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Device.getAndroidVersionCode()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.FULL_SUPPORT}>`getAndroidVersionCode`</AppCompatibilityItem>
    </AppCompatibility>
</Section>