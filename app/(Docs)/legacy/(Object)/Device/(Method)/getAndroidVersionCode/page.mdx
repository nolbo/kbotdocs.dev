import {compatibility} from "@/constants/compatibility/legacy/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getAndroidVersionCode() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.getAndroidVersionCode() - 레거시 API"
    }
}

<Section>
    ## Device.getAndroidVersionCode()

    앱 구동 환경의 안드로이드 버전 코드를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getAndroidVersionCode();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱 구동 환경의 안드로이드 버전 코드를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getAndroidVersionCode"]}/>
</Section>