import {compatibility} from "@/constants/compatibility/legacy/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getApiLevel() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.getApiLevel() - 레거시 API"
    }
}

<Section>
    # Device.getApiLevel()

    앱 구동 환경의 안드로이드 API Level을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getApiLevel();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱 구동 환경의 안드로이드 API Level을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getApiLevel"]}/>
</Section>