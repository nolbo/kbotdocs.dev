import {compatibility} from "@/constants/compatibility/legacy/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getTotalMemory() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.getTotalMemory() - 레거시 API"
    }
}

<Section>
    # Device.getTotalMemory()

    앱에 할당된 총 메모리 크기(Byte)를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getTotalMemory();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱에 할당된 총 메모리 크기(Byte)를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getTotalMemory"]}/>
</Section>