import {compatibility} from "@/constants/compatibility/legacy/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getPhoneModel() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.getPhoneModel() - 레거시 API"
    }
}

<Section>
    ## Device.getPhoneModel()

    앱 구동 기기의 모델명을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getPhoneModel();
    ~~~

    <Section>
        ### 반환값

        `String` - 앱 구동 기기의 모델명을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getPhoneModel"]}/>
</Section>