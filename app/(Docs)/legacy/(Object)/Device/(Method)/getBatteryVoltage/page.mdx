import {compatibility} from "@/constants/compatibility/legacy/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getBatteryVoltage() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.getBatteryVoltage() - 레거시 API"
    }
}

<Section>
    # Device.getBatteryVoltage()

    앱 구동 기기의 배터리 전압(mV)을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getBatteryVoltage();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱 구동 기기의 배터리 전압(mV)을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getBatteryVoltage"]}/>
</Section>