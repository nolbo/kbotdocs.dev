import {compatibility} from "@/constants/compatibility/legacy/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.isScreenOn() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.isScreenOn() - 레거시 API"
    }
}

<Section>
    ## Device.isScreenOn()

    앱 구동 기기의 화면이 켜져 있는지의 여부를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.isScreenOn();
    ~~~

    <Section>
        ### 반환값

        `Boolean` - 앱 구동 기기의 화면이 켜져 있을 경우 `true`, 꺼져 있을 경우 `false`를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["isScreenOn"]}/>
</Section>