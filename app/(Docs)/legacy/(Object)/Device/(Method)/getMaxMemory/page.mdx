import {compatibility} from "@/constants/compatibility/legacy/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getMaxMemory() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.getMaxMemory() - 레거시 API"
    }
}

<Section>
    # Device.getMaxMemory()

    앱이 사용하려고 시도하는 최대 메모리 크기(Byte)를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getMaxMemory();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱이 사용하려고 시도하는 최대 메모리 크기(Byte)를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getMaxMemory"]}/>
</Section>