import {compatibility} from "@/constants/compatibility/legacy/argument/ImageDB";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ImageDB.getRoomBitmap() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "ImageDB.getRoomBitmap() - 레거시 API"
    }
}

<Section>
    # ImageDB.getRoomBitmap()

    방 아이콘 이미지에 대한 `android.graphics.Bitmap` 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getRoomBitmap();
    ~~~

    <Section>
        ### 반환값

        `android.graphics.Bitmap` - 방 아이콘 이미지에 대한 `android.graphics.Bitmap` 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getRoomBitmap"]}/>
</Section>