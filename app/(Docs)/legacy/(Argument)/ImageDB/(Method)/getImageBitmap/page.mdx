import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ImageDB.getImageBitmap() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "ImageDB.getImageBitmap() - 레거시 API"
    }
}

<Section>
    ## ImageDB.getImageBitmap()

    수신한 이미지에 대한 `android.graphics.Bitmap` 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getImageBitmap();
    ~~~

    <Section>
        ### 반환값

        `android.graphics.Bitmap` - 수신한 이미지에 대한 `android.graphics.Bitmap` 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="ImageDB.getImageBitmap()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getImageBitmap`</AppCompatibilityItem>
    </AppCompatibility>
</Section>