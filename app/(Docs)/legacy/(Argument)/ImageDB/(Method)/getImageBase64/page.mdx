import {compatibility} from "@/constants/compatibility/legacy/argument/ImageDB";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ImageDB.getImageBase64() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "ImageDB.getImageBase64() - 레거시 API"
    }
}

<Section>
    # ImageDB.getImageBase64()

    수신한 이미지를 Base64 인코딩한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getImageBase64();
    ~~~

    <Section>
        ### 반환값

        `String` - 수신한 이미지를 Base64 인코딩한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getImageBase64"]}/>
</Section>