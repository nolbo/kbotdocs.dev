import {compatibility} from "@/constants/compatibility/legacy/argument/ImageDB";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ImageDB.getProfileHash() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "ImageDB.getProfileHash() - 레거시 API"
    }
}

<Section>
    # ImageDB.getProfileHash()

    발신자의 프로필 이미지의 해시코드를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getProfileHash();
    ~~~

    <Section>
        ### 반환값

        `String` - 발신자의 프로필 이미지의 해시코드를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getProfileHash"]}/>
</Section>