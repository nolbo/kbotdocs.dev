import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ImageDB.getProfileHash() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "ImageDB.getProfileHash() - 레거시 API"
    }
}

<Section>
    ## ImageDB.getProfileHash()

    발신자의 프로필 이미지의 해시코드를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getProfileHash();
    ~~~

    <Section>
        ### 반환값

        `String` - 발신자의 프로필 이미지의 해시코드를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="ImageDB.getProfileHash()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.FULL_SUPPORT}>`getProfileHash`</AppCompatibilityItem>
    </AppCompatibility>
</Section>