import {compatibility} from "@/constants/compatibility/legacy/argument/ImageDB";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ImageDB.getProfileMD5() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "ImageDB.getProfileMD5() - 레거시 API"
    }
}

<Section>
    # ImageDB.getProfileMD5()

    발신자의 프로필 이미지를 MD5 인코딩한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getProfileMD5();
    ~~~

    <Section>
        ### 반환값

        `String` - 발신자의 프로필 이미지를 MD5 인코딩한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getProfileMD5"]}/>
</Section>