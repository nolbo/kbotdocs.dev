import {compatibility} from "@/constants/compatibility/legacy/eventListener/onStartCompile";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "onStartCompile 이벤트 리스너 - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "onStartCompile 이벤트 리스너 - 레거시 API"
    }
}

<Section>
    # onStartCompile

    `onStartCompile`은 컴파일 요청을 감지했을 때 컴파일 시작 전에 동작하는 이벤트 리스너입니다.
</Section>
<Section>
    ## 구문

    ~~~js
    function onStartCompile() {

    }
    ~~~
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={Object.values(compatibility)}/>
</Section>