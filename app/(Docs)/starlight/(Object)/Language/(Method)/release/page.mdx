import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Language.release() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Language.release() - StarLight"
    }
}

<Section>
    # Language.release()

    컴파일된 스코프를 사용 해제 상태로 변경합니다. 사용 해제된 스코프는 불필요한 리소스를 모두 정리하고 대기 상태로 전환됩니다.
</Section>
<Section>
    ## 구문

    ~~~js
    language.release(scope: Any);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`scope`</Dt>
            <Dd>
                사용 해제할 스코프
            </Dd>
        </Dl>
    </Section>
</Section>