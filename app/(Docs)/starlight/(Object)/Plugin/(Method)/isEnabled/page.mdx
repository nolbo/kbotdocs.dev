import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Platform.isEnabled() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Platform.isEnabled() - StarLight"
    }
}

<Section>
    # Plugin.isEnabled()

    플러그인의 활성화 여부를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Plugin.isEnabled();
    ~~~

    <Section>
        ### 반환값

        `Boolean` - 플러그인이 활성화 상태일 경우 `true`, 비활성화 상태일 경우 `false`를 반환합니다.
    </Section>
</Section>