import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Languages.getSelf() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Languages.getSelf() - StarLight"
    }
}

<Section>
    # Languages.getSelf()

    프로젝트에 사용되는 언어에 대한 [`Language`](/starlight/Language) 객체를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Languages.getSelf();
    ~~~

    <Section>
        ### 반환값

        `Language` - 프로젝트에 사용되는 언어에 대한 `Language` 객체를 반환합니다.
    </Section>
</Section>