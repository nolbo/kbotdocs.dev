import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Languages - StarLight",
    openGraph: {
        ...openGraph,
        title: "Languages - StarLight"
    }
}

<Section>
    # Languages

    `Languages` 객체는 앱이 지원하는 프로그래밍 언어에 접근하기 위한 객체입니다.
</Section>
<Section>
    ## 메소드

    <Table>
        <Thead>
            <Tr>
                <Th colSpan="2">`Languages` 객체의 메소드</Th>
            </Tr>
        </Thead>
        <Tbody>
            <Tr>
                <Td><span>[`Language`](/starlight/Language)</span></Td>
                <Td>
                    [`getSelf`](/starlight/Languages/getSelf)`()` \
                    프로젝트에 사용되는 언어에 대한 `Language` 객체를 반환합니다.
                </Td>
            </Tr>
            <Tr>
                <Td><span>[`Language`](/starlight/Language)</span></Td>
                <Td>
                    [`ofId`](/starlight/Languages/ofId)`(id: String)` \
                    특정 ID로 식별되는 프로그래밍 언어 구현체에 대한 `Language` 객체를 반환합니다.
                </Td>
            </Tr>
        </Tbody>
    </Table>
</Section>