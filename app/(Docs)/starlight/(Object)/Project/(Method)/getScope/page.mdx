import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Project.getScope() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Project.getScope() - StarLight"
    }
}

<Section>
    # Project.getScope()

    scope 인스턴스를 반환합니다. 프로젝트가 컴파일되지 않은 경우 `null`을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Project.getScope();
    ~~~

    <Section>
        ### 반환값

        `Any?` - 프로젝트가 컴파일된 상태일 경우 scope 인스턴스를, 컴파일되지 않은 상태일 경우 `null`을 반환합니다.
    </Section>
</Section>