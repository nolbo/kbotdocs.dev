import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Platform.getVersion() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Platform.getVersion() - StarLight"
    }
}

<Section>
    # Platform.getVersion()

    현재 구동 중인 봇 앱의 버전을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Platform.getVersion();
    ~~~

    <Section>
        ### 반환값

        `String` - 현재 구동 중인 봇 앱의 버전을 반환합니다.
    </Section>
</Section>