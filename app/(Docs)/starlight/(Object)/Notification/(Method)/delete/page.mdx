import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Notification.delete() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Notification.delete() - StarLight"
    }
}

<Section>
    # Notification.delete()

    알림을 상단바에서 제거합니다. 이미 알림이 제거된 상태일 경우 `java.lang.IllegalStateException`을 throw합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Notification.delete();
    ~~~
</Section>