import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Plugins.getAll() - StarLight",
    openGraph: {
        ...openGraph,
        title: "Plugins.getAll() - StarLight"
    }
}

<Section>
    # Plugins.getAll()

    로드된 모든 플러그인에 대한 `Plugin` 객체를 모은 `java.util.List`를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Plugins.getAll();
    ~~~

    <Section>
        ### 반환값

        `java.util.List<Plugin>` - 로드된 모든 플러그인에 대한 `Plugin` 객체를 모은 `java.util.List`를 반환합니다.
    </Section>
</Section>