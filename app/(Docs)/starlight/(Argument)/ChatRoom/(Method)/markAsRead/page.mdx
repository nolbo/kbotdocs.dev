import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ChatRoom.markAsRead() - StarLight",
    openGraph: {
        ...openGraph,
        title: "ChatRoom.markAsRead() - StarLight"
    }
}

<Section>
    # ChatRoom.markAsRead()

    방의 알림을 읽음 처리합니다. 읽음 처리의 성공 여부를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    room.markAsRead();
    ~~~

    <Section>
        ### 반환값

        `Boolean` - 읽음 처리에 성공할 경우 `true`, 실패할 경우 `false`를 반환합니다.
    </Section>
</Section>