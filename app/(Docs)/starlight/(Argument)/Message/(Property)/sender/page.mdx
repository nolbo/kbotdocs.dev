import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Message.sender - StarLight",
    openGraph: {
        ...openGraph,
        title: "Message.sender - StarLight"
    }
}

<Section>
    # Message.sender

    메시지의 발신자에 대한 [`ChatSender`](/starlight/ChatSender) 객체입니다.
</Section>
<Section>
    ## 구문

    ~~~js
    msg.sender;
    ~~~

    <Section>
        ### 값

        `ChatSender` - 메시지의 발신자에 대한 `ChatSender` 객체입니다.
    </Section>
</Section>