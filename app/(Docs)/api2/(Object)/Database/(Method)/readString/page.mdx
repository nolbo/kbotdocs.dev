import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Database.readString() - API2",
    openGraph: {
        ...openGraph,
        title: "Database.readString() - API2"
    }
}

<Section>
    ## Database.readString()

    `fileName` 이름의 파일의 내용을 문자열로 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Database.readString(fileName: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`fileName`</Dt>
            <Dd>
                내용을 문자열로 읽을 파일 이름
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - `fileName` 이름의 파일의 내용을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Database.readString()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`readString`</AppCompatibilityItem>
    </AppCompatibility>
</Section>