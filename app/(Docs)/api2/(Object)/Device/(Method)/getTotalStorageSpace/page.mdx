import {compatibility} from "@/constants/compatibility/api2/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getTotalStorageSpace() - API2",
    openGraph: {
        ...openGraph,
        title: "Device.getTotalStorageSpace() - API2"
    }
}

<Section>
    # Device.getTotalStorageSpace()

    `path` 경로의 파티션의 전체 크기(Byte)를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getTotalStorageSpace(path: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`path`</Dt>
            <Dd>
                전체 크기를 확인할 파티션의 경로
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `Number` - `path` 경로의 파티션의 전체 크기(Byte)를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getTotalStorageSpace"]}/>
</Section>