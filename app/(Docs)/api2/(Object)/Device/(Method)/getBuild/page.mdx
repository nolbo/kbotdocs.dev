import {compatibility} from "@/constants/compatibility/api2/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getBuild() - API2",
    openGraph: {
        ...openGraph,
        title: "Device.getBuild() - API2"
    }
}

<Section>
    ## Device.getBuild()

    [`android.os.Build`](https://developer.android.com/reference/android/os/Build)를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getBuild();
    ~~~

    <Section>
        ### 반환값

        [`android.os.Build`](https://developer.android.com/reference/android/os/Build) - [`android.os.Build`](https://developer.android.com/reference/android/os/Build)를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getBuild"]}/>
</Section>