import {compatibility} from "@/constants/compatibility/api2/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getBatteryTemperature() - API2",
    openGraph: {
        ...openGraph,
        title: "Device.getBatteryTemperature() - API2"
    }
}

<Section>
    # Device.getBatteryTemperature()

    앱 구동 기기의 배터리 온도(*`temp`*` * 10`, ˚C)를 반환합니다. 해당 메소드는 채팅 자동응답 봇에서 지원하지 않으며 [`Device.getBatteryTemp()`](/api2/Device/getBatteryTemp)로 대체할 수 있습니다. `Device.getBatteryTemp()`의 반환값은 `(`*`temp`*` * 10)`이 아님을 유의하세요.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getBatteryTemperature();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱 구동 기기의 배터리 온도(*`temp`*` * 10`, ˚C)를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getBatteryTemperature"]}/>
</Section>