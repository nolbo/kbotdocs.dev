import {compatibility} from "@/constants/compatibility/api2/object/Device";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getBatteryLevel() - API2",
    openGraph: {
        ...openGraph,
        title: "Device.getBatteryLevel() - API2"
    }
}

<Section>
    # Device.getBatteryLevel()

    앱 구동 기기의 배터리 잔량(%)을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getBatteryLevel();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱 구동 기기의 배터리 잔량(%)을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getBatteryLevel"]}/>
</Section>