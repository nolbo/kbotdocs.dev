import {compatibility} from "@/constants/compatibility/api2/object/BotManager";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "BotManager.compileAll() - API2",
    openGraph: {
        ...openGraph,
        title: "BotManager.compileAll() - API2"
    }
}

<Section>
    # BotManager.compileAll()

    각 `Bot` 객체에 대응하는 모든 스크립트를 컴파일합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    BotManager.compileAll();
    ~~~
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["compileAll"]}/>
</Section>