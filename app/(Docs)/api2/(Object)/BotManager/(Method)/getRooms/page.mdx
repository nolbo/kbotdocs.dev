import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "BotManager.getRooms() - API2",
    openGraph: {
        ...openGraph,
        title: "BotManager.getRooms() - API2"
    }
}

<Section>
    ## BotManager.getRooms()

    메시지 전송이 가능한 방 이름을 배열로 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    BotManager.getRooms(packageName?: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`packageName?`</Dt>
            <Dd>
                메시지가 발신된 방들의 메신저 앱 패키지명
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String[]` - `packageName?` 인자가 존재할 경우 `packageName?`의 메신저 앱에 대해 메시지 전송이 가능한 방 이름이 담긴 배열을 반환합니다.
        `packageName?` 인자가 존재하지 않을 경우 모든 메신저 앱에 대해 메시지 전송이 가능한 방 이름이 담긴 배열을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="BotManager.getRooms()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getRooms`</AppCompatibilityItem>
    </AppCompatibility>
</Section>