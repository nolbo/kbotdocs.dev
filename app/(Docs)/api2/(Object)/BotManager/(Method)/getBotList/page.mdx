import {compatibility} from "@/constants/compatibility/api2/object/BotManager";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "BotManager.getBotList() - API2",
    openGraph: {
        ...openGraph,
        title: "BotManager.getBotList() - API2"
    }
}

<Section>
    # BotManager.getBotList()

    모든 `Bot` 객체 인스턴스를 배열로 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    BotManager.getBotList();
    ~~~

    <Section>
        ### 반환값

        `Bot[]` - 모든 `Bot` 객체 인스턴스를 모은 배열을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getBotList"]}/>
</Section>