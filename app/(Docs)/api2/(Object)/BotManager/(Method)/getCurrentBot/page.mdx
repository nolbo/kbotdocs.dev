import {compatibility} from "@/constants/compatibility/api2/object/BotManager";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "BotManager.getCurrentBot() - API2",
    openGraph: {
        ...openGraph,
        title: "BotManager.getCurrentBot() - API2"
    }
}

<Section>
    # BotManager.getCurrentBot()

    해당 메소드를 호출한 스크립트에 할당된 `Bot` 객체를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    BotManager.getCurrentBot();
    ~~~

    <Section>
        ### 반환값

        `Bot` - 해당 메소드를 호출한 스크립트에 할당된 `Bot` 객체를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getCurrentBot"]}/>
</Section>