import {compatibility} from "@/constants/compatibility/api2/object/AppData";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "AppData.getBoolean() - API2",
    openGraph: {
        ...openGraph,
        title: "AppData.getBoolean() - API2"
    }
}

<Section>
    ## AppData.getBoolean()

    `key`에 대응하는 `Boolean`형 데이터를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    AppData.getBoolean(key: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`key`</Dt>
            <Dd>
                불러올 `Boolean`형 데이터에 대응하는 키
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `Boolean` - `key`에 대응하는 `Boolean`형 데이터를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["getBoolean"]}/>
</Section>