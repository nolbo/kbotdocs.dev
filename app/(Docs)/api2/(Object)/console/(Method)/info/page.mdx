import {compatibility} from "@/constants/compatibility/api2/object/console";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "console.info() - API2",
    openGraph: {
        ...openGraph,
        title: "console.info() - API2"
    }
}

<FeatureStatusNoti featureStatus={compatibility["info"].status} />
<Section>
    ## console.info()

    정보 로그를 작성합니다. `showToast?`의 값이 `true`일 경우 작성된 로그를 토스트 메시지로 출력합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    console.info(str: String, showToast?: Boolean = false);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`str`</Dt>
            <Dd>
                작성할 정보 로그의 내용
            </Dd>
            <Dt>`showToast?`</Dt>
            <Dd>
                작성된 로그를 토스트 메시지로 출력할 것인지의 여부
            </Dd>
        </Dl>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["info"]}/>
</Section>