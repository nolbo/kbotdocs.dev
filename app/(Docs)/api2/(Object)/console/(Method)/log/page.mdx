import {compatibility} from "@/constants/compatibility/api2/object/console";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "console.log() - API2",
    openGraph: {
        ...openGraph,
        title: "console.log() - API2"
    }
}

<FeatureStatusNoti featureStatus={compatibility["log"].status} />
<Section>
    ## console.log()

    정보 로그를 작성합니다. 해당 메소드의 동작은 [`console.info`](/api2/console/info)`(str, false)`와 같습니다.
</Section>
<Section>
    ## 구문

    ~~~js
    console.log(str: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`str`</Dt>
            <Dd>
                작성할 정보 로그의 내용
            </Dd>
        </Dl>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["log"]}/>
</Section>