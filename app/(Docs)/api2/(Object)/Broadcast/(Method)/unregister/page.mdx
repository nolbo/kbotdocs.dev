import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Broadcast.unregister() - API2",
    openGraph: {
        ...openGraph,
        title: "Broadcast.unregister() - API2"
    }
}

<Section>
    ## Broadcast.unregister()

    `broadcastName`를 이름으로 하는 브로드캐스트에 대한 특정 리스너를 제거합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Broadcast.unregister(broadcastName: String, task: (value: Any) => Any);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`broadcastName`</Dt>
            <Dd>
                리스너를 제거할 브로드캐스트 이름
            </Dd>
            <Dt>`task`</Dt>
            <Dd>
                제거할 리스너
                - `value`: 브로드캐스트을 통해 송출된 값
            </Dd>
        </Dl>
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Broadcast.unregister()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.FULL_SUPPORT}>`unregister`</AppCompatibilityItem>
    </AppCompatibility>
</Section>