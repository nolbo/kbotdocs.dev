import {compatibility} from "@/constants/compatibility/api2/object/Broadcast";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Broadcast.unregisterAll() - API2",
    openGraph: {
        ...openGraph,
        title: "Broadcast.unregisterAll() - API2"
    }
}

<Section>
    ## Broadcast.unregisterAll()

    각 브로드캐스트에 등록된 모든 리스너를 제거합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Broadcast.unregisterAll();
    ~~~
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["unregisterAll"]}/>
</Section>