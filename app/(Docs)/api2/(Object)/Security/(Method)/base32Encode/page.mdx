import {compatibility} from "@/constants/compatibility/api2/object/Security";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Security.base32Encode() - API2",
    openGraph: {
        ...openGraph,
        title: "Security.base32Encode() - API2"
    }
}

<Section>
    # Security.base32Encode()

    `value`를 Base32 암호화한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Security.base32Encode(value: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`value`</Dt>
            <Dd>
                암호화할 값
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - `value`를 Base32 암호화한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["base32Encode"]}/>
</Section>