import {compatibility} from "@/constants/compatibility/api2/object/Security";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Security.ariaEncode() - API2",
    openGraph: {
        ...openGraph,
        title: "Security.ariaEncode() - API2"
    }
}

<Section>
    # Security.ariaEncode()

    `value`를 ARIA 암호화한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Security.ariaEncode(key: String, value: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`key`</Dt>
            <Dd>
                ARIA 암호화를 위한 키
            </Dd>
            <Dt>`value`</Dt>
            <Dd>
                암호화할 값
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - `value`를 ARIA 암호화한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["ariaEncode"]}/>
</Section>