import {compatibility} from "@/constants/compatibility/api2/object/Http";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Http - API2",
    openGraph: {
        ...openGraph,
        title: "Http - API2"
    }
}

<Section>
    # Http

    `Http` 객체는 웹 상에서의 request(리퀘스트; 요청)에 관한 기능을 제공하는 객체입니다.
</Section>
<Section>
    ## 메소드

    <Table>
        <Thead>
            <Tr>
                <Th colSpan="2">`Http` 객체의 메소드</Th>
            </Tr>
        </Thead>
        <Tbody>
            <Tr>
                <Td><span>`void`</span></Td>
                <Td>
                    [`request`](/api2/Http/request(Object,Function))`(option: Object, callBack: (error: java.lang.Exception?, response: org.jsoup.Connection.Response?, doc: org.jsoup.nodes.Document?) => Any)` \
                    [`jsoup`](https://jsoup.org/)을 이용해 옵션에 따라 비동기 request를 보냅니다.
                </Td>
            </Tr>
            <Tr>
                <Td><span>`void`</span></Td>
                <Td>
                    [`request`](/api2/Http/request(String,Function))`(url: String, callBack: (error: java.lang.Exception?, response: org.jsoup.Connection.Response?, doc: org.jsoup.nodes.Document?) => Any)` \
                    [`jsoup`](https://jsoup.org/)을 이용해 `url`에 비동기 request를 보냅니다.
                </Td>
            </Tr>
            <Tr>
                <Td><span>[`org.jsoup.nodes.Document`](https://jsoup.org/apidocs/org/jsoup/nodes/Document.html)</span></Td>
                <Td>
                    [`requestSync`](/api2/Http/requestSync(Object))`(option: Object)` \
                    [`jsoup`](https://jsoup.org/)을 이용해 옵션에 따라 동기 request를 보냅니다. 요청에 대한 응답을 `org.jsoup.nodes.Document` 형식으로 반환합니다.
                </Td>
            </Tr>
            <Tr>
                <Td><span>[`org.jsoup.nodes.Document`](https://jsoup.org/apidocs/org/jsoup/nodes/Document.html)</span></Td>
                <Td>
                    [`requestSync`](/api2/Http/requestSync(String))`(url: String)` \
                    [`jsoup`](https://jsoup.org/)을 이용해 `url`에 동기 request를 보냅니다. 요청에 대한 응답을 `org.jsoup.nodes.Document` 형식으로 반환합니다.
                </Td>
            </Tr>
        </Tbody>
    </Table>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={Object.values(compatibility)}/>
</Section>