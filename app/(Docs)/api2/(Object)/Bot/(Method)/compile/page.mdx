import {compatibility} from "@/constants/compatibility/api2/object/Bot";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Bot.compile() - API2",
    openGraph: {
        ...openGraph,
        title: "Bot.compile() - API2"
    }
}

<Section>
    ## Bot.compile()

    `Bot`객체에 대응하는 스크립트를 컴파일합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    bot.compile();
    ~~~
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["compile"]}/>
</Section>