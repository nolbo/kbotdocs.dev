import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Bot.getPower() - API2",
    openGraph: {
        ...openGraph,
        title: "Bot.getPower() - API2"
    }
}

<Section>
    ## Bot.getPower()

    `Bot`객체에 대응하는 스크립트의 활성화 여부를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    bot.getPower();
    ~~~

    <Section>
        ### 반환값

        `Boolean` - 스크립트가 활성화 상태일 경우 `true`, 비활성화 상태일 경우 `false`를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Bot.getPower()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getPower`</AppCompatibilityItem>
    </AppCompatibility>
</Section>