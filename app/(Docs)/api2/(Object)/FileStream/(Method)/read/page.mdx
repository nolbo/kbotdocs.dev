import {compatibility} from "@/constants/compatibility/api2/object/FileStream";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "FileStream.read() - API2",
    openGraph: {
        ...openGraph,
        title: "FileStream.read() - API2"
    }
}

<Section>
    ## FileStream.read()

    `path` 경로의 파일 내용을 문자열로 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    FileStream.read(path: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`path`</Dt>
            <Dd>
                내용을 문자열로 읽을 파일의 경로
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - `path` 경로의 파일 내용을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompat compats={compatibility["read"]}/>
</Section>