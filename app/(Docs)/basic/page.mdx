import {openGraph} from "@/constants/metadata";
import {Docs} from "@/constants/docs";
import Link from "next/link";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "기초 자습서",
    openGraph: {
        ...openGraph,
        title: "기초 자습서"
    }
}

<Section>
    # 개요

    해당 문서는 카카오톡봇과 JavaScript를 처음 접하는 사람들을 위한 기초 자습서입니다. 카카오톡봇 개발에 보편적으로 사용되는 프로그래밍 언어인 JavaScript와 카카오톡봇 개발을 위한 필수적인 API를 학습합니다.
</Section>
<Section>
    <ol className={"flex flex-col gap-[16px]"}>
        {
            Docs[3].children.map((e, i) => (
                <li key={i} className={"p-[8px_12px] rounded-md border border-layer1 bg-layer1"}><Link href={e.path}>{e.label}</Link></li>
            ))
        }
    </ol>
</Section>
<Section>
    <MileStone next={Docs[3].children[0]} />
</Section>