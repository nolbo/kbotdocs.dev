import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "Security.seedDecode() - API2",
    openGraph: {
        title: "Security.seedDecode() - API2"
    }
}

<Section>
    ## Security.seedDecode()

    `value`를 SEED 복호화한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Security.seedDecode(key: String, value: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`key`</Dt>
            <Dd>
                SEED 복호화를 위한 키
            </Dd>
            <Dt>`value`</Dt>
            <Dd>
                복호화할 값
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - `value`를 SEED 복호화한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Security.seedDecode()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`seedDecode`</AppCompatibilityItem>
    </AppCompatibility>
</Section>