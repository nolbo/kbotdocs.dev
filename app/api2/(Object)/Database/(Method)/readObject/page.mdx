import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "Database.readObject() - API2",
    openGraph: {
        title: "Database.readObject() - API2"
    }
}

<Section>
    ## Database.readObject()

    `fileName` 이름의 파일의 내용을 `Object` 형식으로 반환합니다. 파일 내용의 형식이 `JSON`일 경우에만 가능합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Database.readObject(fileName: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`fileName`</Dt>
            <Dd>
                내용을 `Object` 형식으로 읽을 파일 이름
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `Object` - `fileName` 이름의 파일의 내용을 `Object` 형식으로 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Database.readObject()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`readObject`</AppCompatibilityItem>
    </AppCompatibility>
</Section>