import { Compatibility } from "@/constants/compatibility";

<Section>
    ## BotManager.getBot()

    `botName` 이름의 `Bot` 객체를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    BotManager.getBot(botName: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`botName`</Dt>
            <Dd>
                반환받을 `Bot` 객체의 이름
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - `botName` 이름의 `Bot` 객체를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="BotManager.getBot()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getBot`</AppCompatibilityItem>
    </AppCompatibility>
</Section>