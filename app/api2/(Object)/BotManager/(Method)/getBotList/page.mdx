import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "BotManager.getBotList() - API2",
    openGraph: {
        title: "BotManager.getBotList() - API2"
    }
}

<Section>
    ## BotManager.getBotList()

    모든 `Bot` 객체 인스턴스를 배열로 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    BotManager.getBotList();
    ~~~

    <Section>
        ### 반환값

        `Bot[]` - 모든 `Bot` 객체 인스턴스를 모은 배열을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="BotManager.getBotList()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getBotList`</AppCompatibilityItem>
    </AppCompatibility>
</Section>