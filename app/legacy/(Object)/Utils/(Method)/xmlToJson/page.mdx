import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "Utils.xmlToJson() - 레거시 API",
    openGraph: {
        title: "Utils.xmlToJson() - 레거시 API"
    }
}

<Section>
    ## Utils.xmlToJson()

    XML 형식의 문자열을 `JSON String`으로 변환한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Utils.xmlToJson(xml: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`xml`</Dt>
            <Dd>
                `JSON String`으로 변환할 XML 형식의 문자열
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - `xml`을 `JSON String`으로 변환한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Utils.xmlToJson()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.NO_SUPPORT} sl={Compatibility.NO_SUPPORT}>`xmlToJson`</AppCompatibilityItem>
    </AppCompatibility>
</Section>