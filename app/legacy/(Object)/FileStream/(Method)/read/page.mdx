import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "FileStream.read() - 레거시 API",
    openGraph: {
        title: "FileStream.read() - 레거시 API"
    }
}

<Section>
    ## FileStream.read()

    `path` 경로의 파일 내용을 문자열로 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    FileStream.read(path: String);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`path`</Dt>
            <Dd>
                내용을 문자열로 읽을 파일의 경로
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `String` - `path` 경로의 파일 내용을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="FileStream.read()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.FULL_SUPPORT}>`read`</AppCompatibilityItem>
    </AppCompatibility>
</Section>