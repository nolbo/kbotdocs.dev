import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "Device.getApiLevel() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "Device.getApiLevel() - 레거시 API"
    }
}

<Section>
    ## Device.getApiLevel()

    앱 구동 환경의 안드로이드 API Level을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getApiLevel();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱 구동 환경의 안드로이드 API Level을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Device.getApiLevel()">
        <AppCompatibilityItem msgBot={Compatibility.NO_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>[`getApiLevel`](/legacy/Device/getApiLevel)</AppCompatibilityItem>
    </AppCompatibility>
</Section>