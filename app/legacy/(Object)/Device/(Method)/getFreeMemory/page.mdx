import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "Device.getFreeMemory() - 레거시 API",
    openGraph: {
        title: "Device.getFreeMemory() - 레거시 API"
    }
}

<Section>
    ## Device.getFreeMemory()

    앱이 사용 가능한 메모리 크기(Byte)를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Device.getFreeMemory();
    ~~~

    <Section>
        ### 반환값

        `Number` - 앱이 사용 가능한 메모리 크기(Byte)를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Device.getFreeMemory()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.FULL_SUPPORT}>`getFreeMemory`</AppCompatibilityItem>
    </AppCompatibility>
</Section>