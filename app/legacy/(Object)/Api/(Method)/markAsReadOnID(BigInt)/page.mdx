import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "Api.markAsReadOnID(BigInt) - 레거시 API",
    openGraph: {
        title: "Api.markAsReadOnID(BigInt) - 레거시 API"
    }
}

<Section>
    ## Api.markAsReadOnID(BigInt)

    특정 ID로 식별되는 방의 메시지를 읽음 처리합니다. 방에 대한 알림 읽기 세션을 찾을 수 있는지의 여부를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    Api.markAsReadOnID(roomId: BigInt);
    ~~~

    <Section>
        ### 매개변수

        <Dl>
            <Dt>`roomId`</Dt>
            <Dd>
                메시지를 읽음 처리할 방의 고유 ID
            </Dd>
        </Dl>
    </Section>
    <Section>
        ### 반환값

        `Boolean` - `roomId`로 식별되는 방에 대한 알림 읽기 세션을 찾을 수 있을 경우 `true`, 찾을 수 없을 경우 `false`를 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="Api.markAsReadOnID(BigInt)">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.NO_SUPPORT} sl={Compatibility.NO_SUPPORT}>`markAsReadOnID(BigInt)`</AppCompatibilityItem>
    </AppCompatibility>
</Section>