import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "ImageDB.getRoomBitmap() - 레거시 API",
    openGraph: {
        title: "ImageDB.getRoomBitmap() - 레거시 API"
    }
}

<Section>
    ## ImageDB.getRoomBitmap()

    방 아이콘 이미지에 대한 `android.graphics.Bitmap` 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getRoomBitmap();
    ~~~

    <Section>
        ### 반환값

        `android.graphics.Bitmap` - 방 아이콘 이미지에 대한 `android.graphics.Bitmap` 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="ImageDB.getRoomBitmap()">
        <AppCompatibilityItem msgBot={Compatibility.NO_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getRoomBitmap`</AppCompatibilityItem>
    </AppCompatibility>
</Section>