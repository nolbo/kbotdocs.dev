import { Compatibility } from "@/constants/compatibility";
import {openGraph} from "@/constants/metadata";

export const metadata = {
    metadataBase: new URL("https://kbotdocs.dev"),
    title: "ImageDB.getImageBase64() - 레거시 API",
    openGraph: {
        ...openGraph,
        title: "ImageDB.getImageBase64() - 레거시 API"
    }
}

<Section>
    ## ImageDB.getImageBase64()

    수신한 이미지를 Base64 인코딩한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getImageBase64();
    ~~~

    <Section>
        ### 반환값

        `String` - 수신한 이미지를 Base64 인코딩한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="ImageDB.getImageBase64()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.NO_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getImageBase64`</AppCompatibilityItem>
    </AppCompatibility>
</Section>