import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "ImageDB.getRoomImage() - 레거시 API",
    openGraph: {
        title: "ImageDB.getRoomImage() - 레거시 API"
    }
}

<Section>
    ## ImageDB.getRoomImage()

    방 아이콘 이미지를 Base64 인코딩한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getRoomImage();
    ~~~

    <Section>
        ### 반환값

        `String` - 방 아이콘 이미지를 Base64 인코딩한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="ImageDB.getRoomImage()">
        <AppCompatibilityItem msgBot={Compatibility.NO_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getRoomImage`</AppCompatibilityItem>
    </AppCompatibility>
</Section>