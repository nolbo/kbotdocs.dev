import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "ImageDB.getImage() - 레거시 API",
    openGraph: {
        title: "ImageDB.getImage() - 레거시 API"
    }
}

<Section>
    ## ImageDB.getImage()

    수신한 이미지를 Base64 인코딩한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getImage();
    ~~~

    <Section>
        ### 반환값

        `String` - 수신한 이미지를 Base64 인코딩한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="ImageDB.getImage()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.FULL_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getImage`</AppCompatibilityItem>
    </AppCompatibility>
</Section>