import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "ImageDB.getProfileBase64() - 레거시 API",
    openGraph: {
        title: "ImageDB.getProfileBase64() - 레거시 API"
    }
}

<Section>
    ## ImageDB.getProfileBase64()

    발신자의 프로필 이미지를 Base64 인코딩한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getProfileBase64();
    ~~~

    <Section>
        ### 반환값

        `String` - 발신자의 프로필 이미지를 Base64 인코딩한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="ImageDB.getProfileBase64()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.NO_SUPPORT} sl={Compatibility.FULL_SUPPORT}>`getProfileBase64`</AppCompatibilityItem>
    </AppCompatibility>
</Section>