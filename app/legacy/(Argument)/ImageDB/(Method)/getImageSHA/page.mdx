import { Compatibility } from "@/constants/compatibility";

export const metadata = {
    title: "ImageDB.getImageSHA() - 레거시 API",
    openGraph: {
        title: "ImageDB.getImageSHA() - 레거시 API"
    }
}

<Section>
    ## ImageDB.getImageSHA()

    수신한 이미지를 SHA 인코딩한 값을 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    imageDB.getImageSHA();
    ~~~

    <Section>
        ### 반환값

        `String` - 수신한 이미지를 SHA 인코딩한 값을 반환합니다.
    </Section>
</Section>
<Section>
    ## 앱 호환성

    <AppCompatibility listTitle="ImageDB.getImageSHA()">
        <AppCompatibilityItem msgBot={Compatibility.FULL_SUPPORT} arBot={Compatibility.NO_SUPPORT} sl={Compatibility.NO_SUPPORT}>`getImageSHA`</AppCompatibilityItem>
    </AppCompatibility>
</Section>